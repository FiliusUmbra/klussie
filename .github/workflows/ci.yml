name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    name: Lint, type-check, test, build
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      # npm ci installs exactly what package-lock.json specifies and fails
      # if the lockfile and package.json disagree. npm install would
      # silently reconcile them, which makes a green run mean less.
      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      # Before Test deliberately: type errors are cheap to detect and the
      # suite is not, so a type error fails the run in seconds rather than
      # after the tests have finished.
      - name: Type-check
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
